[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
surrealdb = { version = "1.0.0", features = ["ws", "macros"] }
anyhow = "1.0"
thiserror = "1.0"

# Add the core-engine as a library
core-engine = { path = "../core-engine" }
